{{-- @if (!true)
    {{ 'true' }}
@else
    {{ 'false' }}
@endif --}}
{{-- @foreach ($articles as $article)
    {{$article}}
@endforeach --}}
{{-- @forelse ($articles as $article)
    @if ($article == $loop->last)
        {{$article." is first argument"}}
    @endif
@empty
    {{'There is no article'}}
@endforelse --}}

